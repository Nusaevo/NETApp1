{
    "private": true,
    "version": "1.0.0",
    "description": "Laravel Livewire + Bootstrap Admin Panel (CDN-only)",
    "scripts": {
        "dev": "echo 'Using CDN-only, no build needed'",
        "build": "echo 'Using CDN-only, no build needed'",
        "hot": "echo 'Using CDN-only, no build needed'"
    },
    "devDependencies": {
        "vite": "^4.5.0"
    }
}
